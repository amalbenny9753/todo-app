<h2 class="mb-4">Verify OTP</h2>

<p class="text-muted mb-3">
  We sent a 6-digit OTP to: <strong><%= email %></strong>
</p>

<form method="POST" action="/verify-otp" class="card p-3 shadow-sm">
  <div class="mb-3">
    <label>Enter OTP</label>
    <input type="text" name="otp" class="form-control" maxlength="6" pattern="[0-9]{6}" required>
    <div class="form-text">Enter the 6-digit code sent to your email</div>
  </div>
  <button type="submit" class="btn btn-primary w-100">Verify OTP</button>
</form>

<p class="mt-3">
  <a href="/forgot-password">Back to Forgot Password</a>
</p>

<% if (errorMessage) { %>
<script>
  Swal.fire({
    toast: true,
    position: "top",
    icon: "error",
    title: "<%= errorMessage %>",
    showConfirmButton: false,
    timer: 3000,
    timerProgressBar: true
  });
</script>
<% } %>